apply plugin: 'maven-publish'

afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                from components.release
                artifactId = 'BaseKit'
                version = '1.0.0'
            }
            developers {
                developer {
                    name = "Jun"
                    email = "tiejunshe@gamil.com"
                }
            }
        }
    }
}

