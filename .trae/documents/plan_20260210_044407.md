我将为您优化 `LogViewerActivity`，增加日志删除功能并优化搜索栏的交互。

### 🚀 优化方案

1.  **日志删除功能**：
    *   **UI 交互**：在顶部菜单栏增加“删除”按钮（垃圾桶图标）。
    *   **逻辑处理**：点击后弹出确认对话框，确认后删除当前选中的日志文件，并自动刷新文件列表，切换到下一个可用文件或清空内容。
    *   **数据层**：调用 `file.delete()` 并在 `LogManager` 中可能需要增加刷新文件列表的方法（或直接在 Activity 中重新获取）。

2.  **搜索栏交互优化**：
    *   **默认隐藏**：`OutlinedTextField` 默认不可见。
    *   **显式开启**：在顶部菜单栏增加“搜索”按钮（放大镜图标）。
    *   **动画过渡**：点击搜索按钮后，搜索栏展开；点击关闭（或清除）后折叠。

### 📋 实施步骤

1.  **修改 `LogViewerActivity.kt`**：
    *   新增 `showSearch` 状态变量。
    *   在 `TopAppBar` actions 中添加 `Search` 和 `Delete` 图标。
    *   实现删除逻辑：删除文件 -> 重新加载文件列表 -> 更新 `selectedFile`。
    *   实现搜索显隐逻辑：点击搜索图标切换 `showSearch`，并清空 `searchQuery`。

2.  **新增确认弹窗**：
    *   使用 `AlertDialog` 确保删除操作的安全性。

### ⚠️ 注意事项
*   删除当前查看的文件后，如果还有其他文件，应自动选中第一个；如果没有文件了，应清空内容并显示空状态。
*   搜索栏显示时，应确保不遮挡其他核心操作。
