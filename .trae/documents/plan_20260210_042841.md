我将为您优化 `LogViewerActivity` 的 UI 和功能。

### 🎨 UI 优化方案
1.  **顶部导航栏 (TopAppBar)**：
    *   新增 **返回按钮**，符合 Android 交互规范。
    *   新增 **设置菜单**，用于弹出日志显示格式配置。
2.  **日志列表优化**：
    *   **分隔线**：增加日志条目间的分隔线，提升可读性。
    *   **等宽字体**：使用 `FontFamily.Monospace`，使日志对齐更整齐。
    *   **自动滚动**：加载新日志时自动滚动到底部（可选）。

### ⚙️ 功能增强方案
1.  **显示设置 (LogDisplayOptions)**：
    *   增加 `showTimestamp` (是否显示时间戳)
    *   增加 `showTag` (是否显示 Tag)
    *   增加 `showLevel` (是否显示 Level)
    *   状态持久化：使用 `remember` 或 `ViewModel` 保存这些 UI 状态。
2.  **日志解析优化**：
    *   在 `LogItem` 中解析原始日志字符串，根据开关动态拼接展示内容，而不是简单显示整行。

### 📋 实施步骤
1.  **修改 `LogViewerActivity.kt`**：
    *   引入 `TopAppBar` 并添加 `navigationIcon`。
    *   在 TopBar 右侧添加 `IconButton` 触发设置弹窗 (`DropdownMenu` 或 `ModalBottomSheet`)。
    *   实现日志内容的动态格式化显示逻辑。

### ⚠️ 注意事项
*   由于日志格式是固定的 `yyyy-MM-dd HH:mm:ss.SSS [LEVEL] TAG: Message`，我们需要简单的正则或字符串分割来提取各部分，以便按需隐藏。
